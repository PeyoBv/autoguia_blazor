@*
    ⚠️ COMPONENTE DESHABILITADO ⚠️
    Este componente no se usa. El login con Google ahora usa Razor Pages tradicionales
    para evitar problemas de AntiforgeryToken.
    Ver: /Pages/Account/ExternalLogin.cshtml
*@

@using Microsoft.AspNetCore.Authentication
@using Microsoft.AspNetCore.Identity
@using Rodavia.Web.Data

@inject SignInManager<ApplicationUser> SignInManager
@inject IdentityRedirectManager RedirectManager

<div class="alert alert-warning">
    <strong>Este componente está deshabilitado.</strong>
    Por favor usa el login desde /Account/Login que redirige a las Razor Pages.
</div>

@code {
    // Componente deshabilitado - No se usa
}
