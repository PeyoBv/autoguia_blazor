# AutoGuía - Docker Compose para desarrollo local
version: '3.8'

services:
  # Solo servicios necesarios para desarrollo
  # Rodavia - Docker Compose para desarrollo local
      
  # Base de datos principal para Rodavia
  rodavia-db:
    image: postgres:15-alpine
    container_name: rodavia-db-dev
    environment:
      POSTGRES_DB: rodavia_dev
      POSTGRES_USER: rodavia_dev
      POSTGRES_PASSWORD: dev123
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - postgres-dev-data:/var/lib/postgresql/data
    ports:
      - "5433:5432"
    networks:
      - rodavia-dev-network
    restart: unless-stopped

  # Base de datos separada para Identity
  identity-db:
    image: postgres:15-alpine
    container_name: identity-db-dev
    environment:
      POSTGRES_DB: identity_dev
      POSTGRES_USER: identity_dev
      POSTGRES_PASSWORD: dev123
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - postgres-identity-data:/var/lib/postgresql/data
    ports:
      - "5434:5432"
    networks:
      - rodavia-dev-network
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: rodavia-redis-dev
    command: redis-server --appendonly yes
    volumes:
      - redis-dev-data:/data
    ports:
      - "6380:6379"
    networks:
      - rodavia-dev-network
    restart: unless-stopped

  # Adminer para desarrollo
  adminer:
    image: adminer:latest
    container_name: rodavia-adminer-dev
    ports:
      - "8080:8080"
    environment:
      ADMINER_DEFAULT_SERVER: rodavia-db
    depends_on:
      - rodavia-db
      - identity-db
    networks:
      - rodavia-dev-network
    restart: unless-stopped

volumes:
  postgres-dev-data:
    driver: local
  postgres-identity-data:
    driver: local
  redis-dev-data:
    driver: local

networks:
  rodavia-dev-network:
    driver: bridge